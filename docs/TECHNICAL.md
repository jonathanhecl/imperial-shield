# Imperial Shield - Documentación Técnica

## Flujo de Trabajo del Sistema

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           IMPERIAL SHIELD v1.0                               │
│                    Sistema de Monitoreo de Seguridad                         │
└─────────────────────────────────────────────────────────────────────────────┘

                                    ┌──────────────┐
                                    │   STARTUP    │
                                    │  (Con Admin) │
                                    └──────┬───────┘
                                           │
                              ┌────────────▼────────────┐
                              │   SingleInstanceCheck   │
                              │   (Mutex Validation)    │
                              └────────────┬────────────┘
                                           │
                    ┌──────────────────────┼──────────────────────┐
                    │                      │                      │
           ┌────────▼────────┐   ┌─────────▼─────────┐   ┌────────▼────────┐
           │  HostsMonitor   │   │  DefenderMonitor  │   │   TaskbarIcon   │
           │ (FileWatcher)   │   │   (WMI + PS)      │   │   (Systray)     │
           └────────┬────────┘   └─────────┬─────────┘   └────────┬────────┘
                    │                      │                      │
                    │ Evento              │ Timer 60s            │ Click
                    │ OnChanged           │ Polling              │ Eventos
                    ▼                      ▼                      ▼
           ┌────────────────┐   ┌────────────────┐      ┌────────────────┐
           │ hosts cambió?  │   │ Defender off?  │      │   Dashboard    │
           │ Hash diferente │   │ Nueva exclusión│      │   Viewers      │
           └───────┬────────┘   └───────┬────────┘      └────────────────┘
                   │                    │
         ┌─────────▼────────────────────▼─────────┐
         │            TOAST NOTIFICATION           │
         │  "El archivo hosts ha sido modificado"  │
         │  [Restaurar] [Ver Detalles]             │
         └─────────────────────────────────────────┘
```

## Módulo 1: Monitor de HOSTS

### Flujo de Detección
```
C:\Windows\System32\drivers\etc\hosts
                │
        FileSystemWatcher
        (NotifyFilters.LastWrite)
                │
                ▼
        ┌───────────────┐
        │ OnChanged()   │
        │ - Lee archivo │
        │ - Calcula SHA │
        │ - Compara     │
        └───────┬───────┘
                │
    ┌───────────┴───────────┐
    │                       │
Hash Igual              Hash Diferente
(Ignorar)               (ALERTA!)
                            │
                    ┌───────▼───────┐
                    │ DetectChanges │
                    │ - Diff líneas │
                    │ - Identificar │
                    │   añadidas/   │
                    │   eliminadas  │
                    └───────┬───────┘
                            │
                    ┌───────▼───────┐
                    │ Notify User   │
                    │ + Oferta      │
                    │   Restaurar   │
                    └───────────────┘
```

### Ejemplo de Ataque Detectado
```
Malware añade:
127.0.0.1 windows.update.com
127.0.0.1 microsoft.com
127.0.0.1 virustotal.com

Imperial Shield:
⚠️ "El archivo HOSTS ha sido modificado"
➕ 3 línea(s) añadida(s):
   127.0.0.1 windows.update.com
   127.0.0.1 microsoft.com
   127.0.0.1 virustotal.com

[Restaurar Backup] [Ver Archivo]
```

## Módulo 2: Monitor de Windows Defender

### Consultas WMI/PowerShell
```powershell
# Estado del antivirus
Get-MpComputerStatus | Select-Object RealTimeProtectionEnabled, 
                                      BehaviorMonitorEnabled,
                                      AntivirusSignatureVersion

# Exclusiones
Get-MpPreference | Select-Object ExclusionPath,
                                  ExclusionExtension,
                                  ExclusionProcess
```

### Lógica de Detección
```
Timer cada 60 segundos
        │
┌───────▼───────┐
│  Query WMI    │
│  SecurityCtr2 │
└───────┬───────┘
        │
        ├─────────────────────────────────┐
        │                                 │
┌───────▼───────┐               ┌─────────▼─────────┐
│ Estado cambió │               │  Nuevas Exclusiones│
│ Defender OFF? │               │  en la lista?      │
└───────┬───────┘               └─────────┬─────────┘
        │                                 │
        ▼                                 ▼
🚨 ALERTA CRÍTICA              🚨 ALERTA
"Defender desactivado"         "Nueva exclusión:
                                C:\Users\...\malware"
```

## Módulo 3: Visor de Procesos

### Clasificación de Amenazas
```
┌─────────────────────────────────────────────────────────────┐
│                    THREAT LEVEL MATRIX                       │
├──────────────┬────────────────┬───────────────┬─────────────┤
│   Proceso    │  Ubicación     │    Firma      │   Nivel     │
├──────────────┼────────────────┼───────────────┼─────────────┤
│ svchost.exe  │ C:\Temp        │   Cualquiera  │ 🔴 CRÍTICO │
│ svchost.exe  │ System32       │   Sin firma   │ 🟠 ALTO    │
│ miapp.exe    │ AppData\Local  │   Sin firma   │ 🟠 ALTO    │
│ chrome.exe   │ Program Files  │   Sin firma   │ 🟡 MEDIO   │
│ chrome.exe   │ Program Files  │   Desconocida │ 🟢 BAJO    │
│ chrome.exe   │ Program Files  │   Google LLC  │ ✅ SEGURO  │
└──────────────┴────────────────┴───────────────┴─────────────┘
```

### Verificación de Firma Digital
```csharp
// Usando X509Certificate
X509Certificate.CreateFromSignedFile(exePath)

// Verificamos:
// 1. ¿Está firmado?
// 2. ¿El certificado es válido (fecha)?
// 3. ¿El emisor es de confianza?
//    - Microsoft
//    - Google
//    - Mozilla
//    - Adobe
//    - etc.
```

## Módulo 4: Monitor de Conexiones

### P/Invoke a GetExtendedTcpTable
```csharp
[DllImport("iphlpapi.dll")]
private static extern uint GetExtendedTcpTable(
    IntPtr pTcpTable,
    ref int dwOutBufLen,
    bool sort,
    int ipVersion,
    TCP_TABLE_CLASS tblClass,
    uint reserved = 0);
```

### Detección de Reverse Shell
```
┌─────────────────────────────────────────────────────────────┐
│               REVERSE SHELL DETECTION                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  powershell.exe ──ESTABLISHED──► IP:443                     │
│       ⬆                                                     │
│   🚨 CRÍTICO: PowerShell NO debería tener conexiones       │
│               activas a servidores externos                 │
│                                                             │
│  cmd.exe ──ESTABLISHED──► IP:4444                           │
│       ⬆                                                     │
│   🚨 CRÍTICO: Puerto 4444 es el default de Metasploit      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Regla de Detección
```
SI proceso EN [powershell, cmd, wscript, cscript, mshta]
   Y estado = ESTABLISHED
   Y remoteIP != localhost
   Y remoteIP != LAN (192.168.*, 10.*, 172.16-31.*)
ENTONCES
   ThreatLevel = CRITICAL
   Mensaje = "Posible Reverse Shell detectado"
```

## Recursos del Sistema

### Consumo de RAM
| Componente            | RAM     |
|-----------------------|---------|
| Base WPF              | ~12 MB  |
| FileSystemWatcher     | ~0.5 MB |
| Timer (WMI)           | ~1 MB   |
| Systray Icon          | ~0.2 MB |
| **Total (reposo)**    | **~15 MB** |

### Consumo de CPU
| Estado                | CPU   |
|-----------------------|-------|
| Reposo (waiting)      | 0%    |
| Evento HOSTS          | <1%   |
| Timer Defender (60s)  | <0.5% |
| Scan Procesos         | 2-5%  |
| Scan Conexiones       | 1-3%  |

---

## Diagrama de Clases

```
┌─────────────────────────────────────────────────────────────────┐
│                            App                                   │
│ ─────────────────────────────────────────────────────────────── │
│ - _notifyIcon: TaskbarIcon                                      │
│ - _hostsMonitor: HostsFileMonitor                               │
│ - _defenderMonitor: DefenderMonitor                             │
│ ─────────────────────────────────────────────────────────────── │
│ + InitializeApplication()                                        │
│ + ShowToastNotification()                                        │
└─────────────────────────────────────────────────────────────────┘
                │
    ┌───────────┴───────────┬──────────────────────┐
    ▼                       ▼                      ▼
┌───────────────┐   ┌─────────────────┐   ┌─────────────────┐
│HostsFileMonitor│  │ DefenderMonitor │   │ ProcessAnalyzer │
├───────────────┤   ├─────────────────┤   ├─────────────────┤
│- _watcher     │   │- _timer         │   │+ GetAllProcesses│
│- _backupPath  │   │- _knownExclusions│  │+ VerifySignature│
├───────────────┤   ├─────────────────┤   │+ KillProcess    │
│+ Start()      │   │+ Start()        │   └─────────────────┘
│+ Stop()       │   │+ Stop()         │
│+ Restore()    │   │+ GetExclusions()│   ┌─────────────────┐
│               │   │+ RemoveExclusion│   │ NetworkMonitor  │
│ <<event>>     │   │                 │   ├─────────────────┤
│ HostsChanged  │   │ <<event>>       │   │+ GetConnections │
└───────────────┘   │ StatusChanged   │   │+ GetSuspicious  │
                    │ ExclusionAdded  │   └─────────────────┘
                    └─────────────────┘
```

---

*Documentación generada para Imperial Shield v1.0*
