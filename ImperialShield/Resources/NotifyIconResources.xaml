<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tb="http://www.hardcodet.net/taskbar">

    <!-- Taskbar Icon Configuration -->
    <tb:TaskbarIcon x:Key="NotifyIcon"
                    IconSource="/Resources/shield.ico"
                    ToolTipText="Imperial Shield - Sistema de Seguridad"
                    DoubleClickCommand="{x:Static local:Commands.ShowDashboard}"
                    xmlns:local="clr-namespace:ImperialShield">
        
        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu>
                <MenuItem Header="🛡️ Imperial Shield" FontWeight="Bold" IsEnabled="False"/>
                <Separator/>
                <MenuItem Header="📊 Abrir Dashboard" Click="ShowDashboard_Click"/>
                <MenuItem Header="🔍 Visor de Procesos" Click="ShowProcessViewer_Click"/>
                <MenuItem Header="🌐 Monitor de Conexiones" Click="ShowNetworkViewer_Click"/>
                <Separator/>
                <MenuItem Header="📄 Ver Archivo HOSTS" Click="ViewHosts_Click"/>
                <MenuItem Header="🛡️ Estado de Defender" Click="ViewDefender_Click"/>
                <Separator/>
                <MenuItem Header="⚙️ Configuración" Click="Settings_Click"/>
                <MenuItem Header="❌ Salir" Click="Exit_Click"/>
            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>
        
    </tb:TaskbarIcon>

</ResourceDictionary>
