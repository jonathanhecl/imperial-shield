<Window x:Class="ImperialShield.Views.StartupManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestor de Inicio - Imperial Shield" 
        Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource ModernWindow}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🚀 Gestor de Inicio" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,0,15,0"/>
                    <Border Background="#2E5077" CornerRadius="12" Padding="12,4" VerticalAlignment="Center" Margin="0,0,10,0">
                        <TextBlock x:Name="AppCountText" Text="0 apps" Foreground="#4DA8DA" FontWeight="Bold"/>
                    </Border>
                    <TextBlock x:Name="SuspiciousCountText" Text="" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Text="Gestiona qué aplicaciones se ejecutan al iniciar Windows. Desactiva las que no necesites para acelerar tu PC." 
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="14" Margin="0,8,0,0"/>
            </StackPanel>
            
            <!-- Legend -->
            <Border Grid.Column="1" Background="#152538" CornerRadius="8" Padding="15,10" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Leyenda:" Foreground="#94A3B8" FontSize="11" FontWeight="Bold" Margin="0,0,0,5"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🛡️ Verificado" Foreground="#4DA8DA" FontSize="11" Margin="0,0,12,0"/>
                        <TextBlock Text="✅ Seguro" Foreground="#27AE60" FontSize="11" Margin="0,0,12,0"/>
                        <TextBlock Text="🟡 Sin firma" Foreground="#F39C12" FontSize="11" Margin="0,0,12,0"/>
                        <TextBlock Text="🔴 Sospechoso" Foreground="#E74C3C" FontSize="11"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Info Tooltip -->
        <Border Grid.Row="1" Background="#1A2E45" CornerRadius="8" Padding="15,10" Margin="0,0,0,15" Cursor="Help">
            <Border.ToolTip>
                <ToolTip Background="#0A0F1E" Foreground="White" BorderBrush="#4DA8DA" BorderThickness="1">
                    <StackPanel Margin="5">
                        <TextBlock Text="Criterios de Análisis" FontWeight="Bold" Margin="0,0,0,5" Foreground="#4DA8DA"/>
                        <TextBlock Text="• Se verifica la firma digital de cada ejecutable." FontSize="11"/>
                        <TextBlock Text="• Ejecutables sin firma en carpetas temporales = Sospechoso." FontSize="11"/>
                        <TextBlock Text="• Imperial Shield siempre es de confianza." FontSize="11"/>
                    </StackPanel>
                </ToolTip>
            </Border.ToolTip>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ⓘ" Foreground="#4DA8DA" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="Haz clic en el nombre de la app para abrir su ubicación. Los procesos activos pueden terminarse desde el menú contextual." 
                           Foreground="#94A3B8" FontSize="12" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- List -->
        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0" Background="#152538">
            <DataGrid x:Name="StartupGrid" Margin="0" SelectionUnit="FullRow" CanUserAddRows="False"
                      Background="Transparent" BorderThickness="0" 
                      RowBackground="Transparent" AlternatingRowBackground="#1A2E45"
                      HeadersVisibility="Column" GridLinesVisibility="None"
                      AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1E3A5F"/>
                        <Setter Property="Foreground" Value="#94A3B8"/>
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#2E5077"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#1E3A5F"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ThreatLevel}" Value="High">
                                <Setter Property="Background" Value="#30E74C3C"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
                
                <DataGrid.Columns>
                    <!-- Threat Level Icon -->
                    <DataGridTemplateColumn Header="⚠️" Width="50">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ThreatIcon}" FontSize="18" HorizontalAlignment="Center"
                                           ToolTip="{Binding ThreatText}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Nombre y Tipo -->
                    <DataGridTemplateColumn Header="Aplicación" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="White" FontSize="14"
                                               Cursor="Hand" TextDecorations="Underline"
                                               MouseLeftButtonDown="OpenLocation_Click" ToolTip="Clic para abrir ubicación"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                        <Border Background="#1E3A5F" CornerRadius="4" Padding="5,2" Margin="0,0,6,0">
                                            <TextBlock Text="{Binding Type}" FontSize="10" Foreground="#4DA8DA"/>
                                        </Border>
                                        <TextBlock Text="{Binding StatusText}" FontSize="11" Foreground="{Binding StatusColor}"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Firma Digital -->
                    <DataGridTemplateColumn Header="Firma Digital" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SignatureText}" Foreground="{Binding SignatureColor}" 
                                           FontSize="12" TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding Publisher}" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Ruta / Comando -->
                    <DataGridTemplateColumn Header="Comando / Ruta" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Command}" Foreground="#94A3B8" 
                                           TextTrimming="CharacterEllipsis" ToolTip="{Binding Command}"
                                           VerticalAlignment="Center" FontSize="12"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Acciones -->
                    <DataGridTemplateColumn Header="Acciones" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <!-- Toggle Button -->
                                    <Button Content="{Binding ActionText}" 
                                            Click="Toggle_Click"
                                            Width="85" Height="28"
                                            FontSize="11" FontWeight="SemiBold"
                                            Cursor="Hand" Margin="0,0,5,0">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="border" CornerRadius="14" Background="{Binding ActionColor}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    
                                    <!-- Kill Button (only if running) -->
                                    <Button Content="❌ Terminar" 
                                            Click="KillProcess_Click"
                                            Width="80" Height="28"
                                            FontSize="10" FontWeight="SemiBold"
                                            Cursor="Hand"
                                            Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="border" CornerRadius="14" Background="#3A5F82">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="border" Property="Background" Value="#E74C3C"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Footer / Status Bar -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="8" Height="8" Fill="#27AE60" Margin="0,0,8,0"/>
                <TextBlock x:Name="LastUpdatedText" Text="Última actualización: --:--" Foreground="#94A3B8" FontSize="13"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="🔄 Actualizar" Click="Refresh_Click" Style="{StaticResource ModernButton}" 
                        Background="Transparent" BorderBrush="#2E5077" BorderThickness="1" Foreground="#4DA8DA"
                        Width="120" Margin="0,0,10,0"/>
                <Button Content="Cerrar" Click="Close_Click" Style="{StaticResource ModernButton}" Width="100"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
