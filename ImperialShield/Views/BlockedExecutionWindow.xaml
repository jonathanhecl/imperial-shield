<Window x:Class="ImperialShield.Views.BlockedExecutionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🛑 EJECUCIÓN BLOQUEADA - Imperial Shield" 
        Width="600" Height="500"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Storyboard x:Key="PulseIcon">
            <DoubleAnimation Storyboard.TargetName="AlertIcon" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="1.0" To="1.1" Duration="0:0:0.6" AutoReverse="True" RepeatBehavior="Forever"/>
            <DoubleAnimation Storyboard.TargetName="AlertIcon" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="1.0" To="1.1" Duration="0:0:0.6" AutoReverse="True" RepeatBehavior="Forever"/>
        </Storyboard>
        
        <Storyboard x:Key="GlowBorder">
            <DoubleAnimation Storyboard.TargetName="GlowEffect" 
                             Storyboard.TargetProperty="Opacity"
                             From="0.4" To="0.9" Duration="0:0:0.5" AutoReverse="True" RepeatBehavior="Forever"/>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource PulseIcon}"/>
            <BeginStoryboard Storyboard="{StaticResource GlowBorder}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border CornerRadius="30" Background="#060D18" BorderBrush="#F39C12" BorderThickness="4" UseLayoutRounding="True">
        <Border.Effect>
            <DropShadowEffect x:Name="GlowEffect" BlurRadius="80" ShadowDepth="0" Opacity="0.8" Color="#F39C12"/>
        </Border.Effect>

        <Grid>
            <!-- Background Grid -->
            <Border Opacity="0.08" Margin="4">
                <Border.Background>
                    <VisualBrush TileMode="Tile" Viewport="0,0,60,60" ViewportUnits="Absolute">
                        <VisualBrush.Visual>
                            <Path Data="M0,0 L60,0 M0,0 L0,60" Stroke="#F39C12" StrokeThickness="0.8" StrokeDashArray="4,4"/>
                        </VisualBrush.Visual>
                    </VisualBrush>
                </Border.Background>
            </Border>
            <!-- Center Glow -->
            <Border>
                <Border.Background>
                    <RadialGradientBrush Center="0.5,0.4" RadiusX="0.5" RadiusY="0.5">
                        <GradientStop Color="#1A1205" Offset="0"/>
                        <GradientStop Color="Transparent" Offset="1"/>
                    </RadialGradientBrush>
                </Border.Background>
            </Border>

            <Grid Margin="35">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Alert Icon -->
            <Image x:Name="AlertIcon" Source="/Resources/shield_alert_icon.png" 
                   Width="100" Height="100" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,15">
                <Image.RenderTransform>
                    <ScaleTransform CenterX="50" CenterY="50"/>
                </Image.RenderTransform>
                <Image.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Color="#F39C12" Opacity="0.9"/>
                </Image.Effect>
            </Image>
            
            <!-- Title -->
            <TextBlock Grid.Row="1" Text="¡EJECUCIÓN BLOQUEADA!" 
                       Foreground="#F39C12" 
                       FontWeight="ExtraBold" FontSize="26" 
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       Margin="0,0,0,15">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="2" Color="Black" Opacity="0.5"/>
                </TextBlock.Effect>
            </TextBlock>
            
            <!-- Message -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <StackPanel VerticalAlignment="Top">
                    <TextBlock Text="Imperial Shield ha interceptado un programa en cuarentena:" 
                               Foreground="#94A3B8" FontSize="14" TextAlignment="Center" Margin="0,0,0,15"/>
                
                <Border Background="#1B2838" CornerRadius="10" Padding="20" Margin="0,0,0,15">
                    <TextBlock x:Name="ExeNameText" Text="malware.exe" 
                               Foreground="#E74C3C" FontSize="22" FontWeight="Bold" 
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               TextTrimming="CharacterEllipsis"
                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                               ToolTipService.InitialShowDelay="0"
                               Cursor="Hand">
                        <TextBlock.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Abrir ubicación del archivo" Click="OpenLocation_Click"/>
                                <MenuItem Header="Abrir gestión de Cuarentena" Click="OpenQuarantine_Click"/>
                            </ContextMenu>
                        </TextBlock.ContextMenu>
                    </TextBlock>
                </Border>
                
                    <TextBlock Text="Este ejecutable está bloqueado por razones de seguridad y NO puede ejecutarse." 
                               Foreground="#708090" FontSize="12" TextAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>
            </ScrollViewer>
            
            <!-- Actions -->
            <StackPanel Grid.Row="3" Orientation="Vertical" HorizontalAlignment="Center">
                <Button x:Name="OpenQuarantineButton" Content="ABRIR CUARENTENA" Click="OpenQuarantine_Click" 
                        Width="420" Height="45" Margin="0,0,0,10"
                        FontSize="14" FontWeight="SemiBold"
                        Background="#1B2838" Foreground="#94A3B8" 
                        BorderThickness="1" BorderBrush="#3A5F82" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderBrush" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <Button Content="ENTENDIDO" Click="Close_Click" 
                        Width="420" Height="45"
                        FontSize="16" FontWeight="Bold"
                        Background="#F39C12" Foreground="Black" 
                        BorderThickness="0" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFBF40"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Border>
</Window>
