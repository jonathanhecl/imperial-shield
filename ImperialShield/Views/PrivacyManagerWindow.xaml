<Window x:Class="ImperialShield.Views.PrivacyManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🛡️ Centro de Privacidad" 
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource ModernWindow}">

    <Window.Resources>
        <LinearGradientBrush x:Key="TabHeaderGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#0F1C2E" Offset="0"/>
            <GradientStop Color="#1E3A5F" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Padding="20" Background="{StaticResource TabHeaderGradient}" BorderBrush="#3A5F82" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="👁️" FontSize="32" Margin="0,0,15,0" VerticalAlignment="Center"/>
                <StackPanel>
                    <TextBlock Text="CENTRO DE PRIVACIDAD" FontSize="20" FontWeight="Bold" Foreground="#4DA8DA"/>
                    <TextBlock Text="Controla qué aplicaciones pueden ver o escucharte" Foreground="#94A3B8"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="10">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" BorderThickness="1,1,1,0" BorderBrush="#3A5F82" CornerRadius="4,4,0,0" Margin="2,0">
                                    <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="12,6"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#4DA8DA" />
                                        <Setter Property="Foreground" Value="Black" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Border" Property="Background" Value="#0F1C2E" />
                                        <Setter Property="Foreground" Value="#94A3B8" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <TabItem Header="📷 CÁMARA">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Border Background="#16202C" Padding="10" CornerRadius="4" Margin="0,0,0,10">
                        <TextBlock Text="Aplicaciones con acceso a tu CÁMARA web" Foreground="#E8F4FC" FontWeight="SemiBold"/>
                    </Border>

                    <DataGrid x:Name="CameraGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                              Background="Transparent" RowBackground="#0D1B2A" AlternatingRowBackground="#162232"
                              HeadersVisibility="Column" GridLinesVisibility="None" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Aplicación" Binding="{Binding DisplayName}" Width="*" Foreground="White"/>
                            <DataGridTextColumn Header="Último Uso" Binding="{Binding LastActivity}" Width="150" Foreground="#B0C4DE"/>
                            <DataGridTemplateColumn Header="Estado" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding StatusText}" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                                            <Setter Property="Foreground" Value="#4DA8DA"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsRunning}" Value="False">
                                                            <Setter Property="Foreground" Value="#708090"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsActiveRisk}" Value="True">
                                                            <Setter Property="Foreground" Value="#E74C3C"/> 
                                                            <Setter Property="Text" Value="🔴 USANDO"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Acciones" Width="200">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Revocar Permiso" Click="Revoke_Click" Margin="0,0,5,0"
                                                    Style="{StaticResource DangerButton}" FontSize="12" Padding="8,4"
                                                    Tag="{Binding}"/>
                                            <Button Content="Matar Proceso" Click="Kill_Click"
                                                    Style="{StaticResource ModernButton}" Background="#3A5F82" FontSize="12" Padding="8,4"
                                                    Visibility="{Binding IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                    Tag="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="🎤 MICRÓFONO">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Background="#16202C" Padding="10" CornerRadius="4" Margin="0,0,0,10">
                        <TextBlock Text="Aplicaciones con acceso a tu MICRÓFONO" Foreground="#E8F4FC" FontWeight="SemiBold"/>
                    </Border>

                    <DataGrid x:Name="MicGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                              Background="Transparent" RowBackground="#0D1B2A" AlternatingRowBackground="#162232"
                              HeadersVisibility="Column" GridLinesVisibility="None" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Aplicación" Binding="{Binding DisplayName}" Width="*" Foreground="White"/>
                            <DataGridTextColumn Header="Último Uso" Binding="{Binding LastActivity}" Width="150" Foreground="#B0C4DE"/>
                            <DataGridTemplateColumn Header="Estado" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding StatusText}" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                                            <Setter Property="Foreground" Value="#4DA8DA"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsRunning}" Value="False">
                                                            <Setter Property="Foreground" Value="#708090"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsActiveRisk}" Value="True">
                                                            <Setter Property="Foreground" Value="#E74C3C"/> 
                                                            <Setter Property="Text" Value="🔴 USANDO"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Acciones" Width="200">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Revocar Permiso" Click="Revoke_Click" Margin="0,0,5,0"
                                                    Style="{StaticResource DangerButton}" FontSize="12" Padding="8,4"
                                                    Tag="{Binding}"/>
                                            <Button Content="Matar Proceso" Click="Kill_Click"
                                                    Style="{StaticResource ModernButton}" Background="#3A5F82" FontSize="12" Padding="8,4"
                                                    Visibility="{Binding IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                    Tag="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
