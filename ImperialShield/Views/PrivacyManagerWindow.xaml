<Window x:Class="ImperialShield.Views.PrivacyManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Centro de Privacidad - Imperial Shield" 
        Height="750" Width="1000"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource ModernWindow}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Filter Toggle Button Style -->
        <Style x:Key="FilterToggle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E3A5F"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#4DA8DA"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1A2744"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Permission Badge Style -->
        <Style x:Key="PermissionBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
        </Style>

        <!-- Action Button Style -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2E4058"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#4DA8DA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Terminate Button Style -->
        <Style x:Key="TerminateBtn" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#2D1F1F"/>
            <Setter Property="BorderBrush" Value="#7F1D1D"/>
            <Setter Property="Foreground" Value="#FCA5A5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A2525"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="🛡️ Centro de Privacidad" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,0,15,0"/>
                <Border Background="#213346" CornerRadius="12" Padding="12,4" VerticalAlignment="Center">
                    <TextBlock Text="Cámara y Micrófono" Foreground="#4DA8DA" FontWeight="Bold" FontSize="12"/>
                </Border>
            </StackPanel>
            <TextBlock Text="Control unificado de acceso a tus dispositivos de audio y video. Filtra y gestiona permisos fácilmente." 
                       Foreground="{StaticResource TextSecondaryBrush}" FontSize="14" Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Filter Bar -->
        <Border Grid.Row="1" Background="#0F172A" CornerRadius="12" Padding="15,12" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal">
                    <ToggleButton x:Name="FilterAll" Content="Todas" Style="{StaticResource FilterToggle}" 
                                  IsChecked="True" Click="Filter_Click"/>
                    <ToggleButton x:Name="FilterInUse" Style="{StaticResource FilterToggle}" Click="Filter_Click">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Fill="#EF4444" Margin="0,0,6,0"/>
                            <TextBlock Text="En Uso"/>
                        </StackPanel>
                    </ToggleButton>
                    <ToggleButton x:Name="FilterRunning" Style="{StaticResource FilterToggle}" Click="Filter_Click">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Fill="#4DA8DA" Margin="0,0,6,0"/>
                            <TextBlock Text="Ejecutando"/>
                        </StackPanel>
                    </ToggleButton>
                    <ToggleButton x:Name="FilterCamera" Content="📷 Cámara" Style="{StaticResource FilterToggle}" Click="Filter_Click"/>
                    <ToggleButton x:Name="FilterMic" Content="🎤 Micrófono" Style="{StaticResource FilterToggle}" Click="Filter_Click"/>
                </StackPanel>

                <!-- Stats -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="StatsText" Text="0 aplicaciones" Foreground="#64748B" FontSize="12" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- App List -->
        <Border Grid.Row="2" Background="#0A0F1E" CornerRadius="12" BorderBrush="#1E293B" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Column Headers -->
                <Border Background="#111827" CornerRadius="12,12,0,0" Padding="20,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="220"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="APLICACIÓN" Foreground="#64748B" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Grid.Column="1" Text="PERMISOS" Foreground="#64748B" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Grid.Column="2" Text="ESTADO" Foreground="#64748B" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Grid.Column="3" Text="SEGURIDAD" Foreground="#64748B" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Grid.Column="4" Text="ACCIONES" Foreground="#64748B" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Scrollable List -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="AppList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#0F172A" BorderBrush="#1E293B" BorderThickness="0,0,0,1" Padding="20,14">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="220"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- App Name -->
                                        <StackPanel VerticalAlignment="Center" Cursor="Hand">
                                            <StackPanel.ContextMenu>
                                                <ContextMenu>
                                                    <MenuItem Header="📂 Abrir ubicación" Click="OpenFolder_Click"/>
                                                    <MenuItem Header="📋 Copiar ruta" Click="CopyPath_Click"/>
                                                </ContextMenu>
                                            </StackPanel.ContextMenu>
                                            <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" Foreground="White" 
                                                       FontSize="13" TextTrimming="CharacterEllipsis"
                                                       MouseLeftButtonDown="OpenFolder_Click"
                                                       ToolTip="Click derecho para opciones"/>
                                            <TextBlock Text="{Binding AppType}" FontSize="10" Foreground="{Binding AppTypeColor}" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>

                                        <!-- Permission Badges -->
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Style="{StaticResource PermissionBadge}" 
                                                    Background="{Binding CameraBadgeBg}">
                                                <TextBlock Text="📷" FontSize="12" Foreground="{Binding CameraBadgeFg}"/>
                                            </Border>
                                            <Border Style="{StaticResource PermissionBadge}" 
                                                    Background="{Binding MicBadgeBg}">
                                                <TextBlock Text="🎤" FontSize="12" Foreground="{Binding MicBadgeFg}"/>
                                            </Border>
                                        </StackPanel>

                                        <!-- Status -->
                                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Ellipse Width="8" Height="8" Fill="{Binding StatusColor}" Margin="0,0,6,0"/>
                                            <TextBlock Text="{Binding StatusText}" FontSize="11" Foreground="{Binding StatusColor}"/>
                                        </StackPanel>

                                        <!-- Security -->
                                        <TextBlock Grid.Column="3" Text="{Binding SecurityText}" 
                                                   Foreground="{Binding SecurityColor}" FontSize="11" 
                                                   VerticalAlignment="Center" FontWeight="Medium"/>

                                        <!-- Actions -->
                                        <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Button Content="🚫 Revocar" Style="{StaticResource ActionButton}" 
                                                    Click="RevokeAll_Click" Tag="{Binding}"
                                                    ToolTip="{Binding RevokeTooltip}"/>
                                            <Button Content="⏹ Terminar" Style="{StaticResource TerminateBtn}" 
                                                    Click="Terminate_Click" Tag="{Binding}"
                                                    Visibility="{Binding IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Empty State -->
                <StackPanel x:Name="EmptyState" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                    <TextBlock Text="✨" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    <TextBlock Text="No hay aplicaciones que coincidan con el filtro" Foreground="#64748B" FontSize="14" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="3" Background="#111827" CornerRadius="8" Padding="20,15" Margin="0,15,0,0" BorderBrush="#1E293B" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="10" Height="10" Fill="#22C55E" Margin="0,0,12,0"/>
                    <TextBlock x:Name="LastUpdatedText" Text="Última actualización: --:--" Foreground="#94A3B8" FontSize="13" FontWeight="Medium"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="🔄 Actualizar" Click="Refresh_Click" Style="{StaticResource ModernButton}"
                            Width="120" Height="38" Margin="0,0,12,0"/>
                    <Button Content="Cerrar" Click="Close_Click" Style="{StaticResource ModernButton}" 
                            Width="100" Height="38"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
