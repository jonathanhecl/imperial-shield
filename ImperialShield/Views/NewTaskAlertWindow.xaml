<Window x:Class="ImperialShield.Views.NewTaskAlertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ALERTA DE SEGURIDAD - Imperial Shield" 
        Width="600" Height="450"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Storyboard x:Key="PulseIcon">
            <DoubleAnimation Storyboard.TargetName="AlertIcon" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="1.0" To="1.1" Duration="0:0:0.8" AutoReverse="True" RepeatBehavior="Forever"/>
            <DoubleAnimation Storyboard.TargetName="AlertIcon" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="1.0" To="1.1" Duration="0:0:0.8" AutoReverse="True" RepeatBehavior="Forever"/>
        </Storyboard>
        
        <Storyboard x:Key="WindowEntrance">
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="0.8" To="1.0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <BackEase Amplitude="1" EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="0.8" To="1.0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <BackEase Amplitude="1" EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>
    
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource WindowEntrance}"/>
            <BeginStoryboard Storyboard="{StaticResource PulseIcon}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border x:Name="MainBorder" CornerRadius="30" Background="#060D18" BorderBrush="#F59E0B" BorderThickness="4" Opacity="0" UseLayoutRounding="True">
        <Border.RenderTransform>
            <ScaleTransform CenterX="300" CenterY="225"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="80" ShadowDepth="0" Opacity="0.8" Color="#F59E0B"/>
        </Border.Effect>

        <Grid>
            <!-- Background Grid -->
            <Border Opacity="0.08" Margin="4">
                <Border.Background>
                    <VisualBrush TileMode="Tile" Viewport="0,0,60,60" ViewportUnits="Absolute">
                        <VisualBrush.Visual>
                            <Path Data="M0,0 L60,0 M0,0 L0,60" Stroke="#F59E0B" StrokeThickness="0.8" StrokeDashArray="4,4"/>
                        </VisualBrush.Visual>
                    </VisualBrush>
                </Border.Background>
            </Border>
            <!-- Center Glow -->
            <Border>
                <Border.Background>
                    <RadialGradientBrush Center="0.5,0.4" RadiusX="0.5" RadiusY="0.5">
                        <GradientStop Color="#1A1205" Offset="0"/>
                        <GradientStop Color="Transparent" Offset="1"/>
                    </RadialGradientBrush>
                </Border.Background>
            </Border>

            <Grid Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
            
            <!-- Alert Icon -->
            <Image x:Name="AlertIcon" Source="/Resources/shield_alert_icon.png" 
                   Width="100" Height="100" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,15">
                <Image.RenderTransform>
                    <ScaleTransform CenterX="50" CenterY="50"/>
                </Image.RenderTransform>
                <Image.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Color="#F43F5E" Opacity="0.9"/>
                </Image.Effect>
            </Image>
            
            <TextBlock Grid.Row="1" Text="NUEVA TAREA PROGRAMADA" FontSize="24" FontWeight="Bold" Foreground="#F59E0B" 
                       HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,5"/>
            <TextBlock Grid.Row="1" Text="Se ha detectado una nueva tarea en el sistema." FontSize="14" Foreground="#94A3B8" 
                       HorizontalAlignment="Center" TextAlignment="Center" Margin="0,35,0,0"/>

            <ScrollViewer Grid.Row="2" Margin="0,20" VerticalScrollBarVisibility="Auto">
                <Border Background="#111827" CornerRadius="12" Padding="20">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="NOMBRE DE TAREA:" FontSize="10" Foreground="#64748B" FontWeight="Bold"/>
                        <TextBlock x:Name="TaskNameText" Text="Unknown Task" FontSize="18" Foreground="White" FontWeight="Bold" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}" Margin="0,0,0,10"/>
                        
                        <TextBlock Text="UBICACIÓN:" FontSize="10" Foreground="#64748B" FontWeight="Bold"/>
                        <TextBlock x:Name="TaskPathText" Text="\" FontSize="14" Foreground="#94A3B8" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                    </StackPanel>
                </Border>
            </ScrollViewer>

            <StackPanel Grid.Row="3" Orientation="Vertical" HorizontalAlignment="Center">
                <Button x:Name="BlockButton" Content="BLOQUEAR TAREA" Width="420" Height="45" Background="#7F1D1D" Foreground="#FECACA"
                        BorderBrush="#EF4444" BorderThickness="1" FontSize="14" FontWeight="Bold" Cursor="Hand"
                        Click="Block_Click" Margin="0,0,0,10">
                     <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="🚫" Margin="0,0,8,0"/>
                                    <ContentPresenter/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#991B1B"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Content="IGNORAR" Width="420" Height="45" 
                        Background="Transparent" Foreground="#94A3B8"
                        BorderThickness="1" BorderBrush="#3A5F82" 
                        FontSize="14" FontWeight="Bold" Cursor="Hand"
                        Click="Ignore_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderBrush" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Border>
</Window>
