<Window x:Class="ImperialShield.Views.NewTaskAlertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ALERTA DE SEGURIDAD - Imperial Shield" 
        Width="600" Height="450"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Storyboard x:Key="WindowEntrance">
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="0.8" To="1.0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <BackEase Amplitude="1" EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="0.8" To="1.0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <BackEase Amplitude="1" EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>
    
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource WindowEntrance}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border x:Name="MainBorder" CornerRadius="20" Background="#0A0F1E" BorderBrush="#F59E0B" BorderThickness="3" Opacity="0" UseLayoutRounding="True">
        <Border.RenderTransform>
            <ScaleTransform CenterX="300" CenterY="225"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.6" Color="#F59E0B"/>
        </Border.Effect>
        
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Alert Icon -->
            <Image Source="/Resources/shield_alert_icon.png" 
                   Width="100" Height="100" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,15">
                <Image.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Color="#F43F5E" Opacity="0.9"/>
                </Image.Effect>
            </Image>
            
            <TextBlock Grid.Row="1" Text="NUEVA TAREA PROGRAMADA" FontSize="24" FontWeight="Bold" Foreground="#F59E0B" 
                       HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,5"/>
            <TextBlock Grid.Row="1" Text="Se ha detectado una nueva tarea en el sistema." FontSize="14" Foreground="#94A3B8" 
                       HorizontalAlignment="Center" TextAlignment="Center" Margin="0,35,0,0"/>

            <Border Grid.Row="2" Background="#111827" CornerRadius="12" Margin="0,20" Padding="20">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="NOMBRE DE TAREA:" FontSize="10" Foreground="#64748B" FontWeight="Bold"/>
                    <TextBlock x:Name="TaskNameText" Text="Unknown Task" FontSize="18" Foreground="White" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,10"/>
                    
                    <TextBlock Text="UBICACIÓN:" FontSize="10" Foreground="#64748B" FontWeight="Bold"/>
                    <TextBlock x:Name="TaskPathText" Text="\" FontSize="14" Foreground="#94A3B8" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Ignorar" Width="140" Height="45" Background="#1E293B" Foreground="#94A3B8"
                        BorderBrush="#334155" BorderThickness="1" FontSize="14" FontWeight="Bold" Cursor="Hand"
                        Click="Ignore_Click" Margin="0,0,10,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#334155"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <Button Content="Bloquear Tarea" Width="180" Height="45" Background="#7F1D1D" Foreground="#FECACA"
                        BorderBrush="#EF4444" BorderThickness="1" FontSize="14" FontWeight="Bold" Cursor="Hand"
                        Click="Block_Click" Margin="10,0,0,0">
                     <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="🚫" Margin="0,0,8,0"/>
                                    <ContentPresenter/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#991B1B"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
