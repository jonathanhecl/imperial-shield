<Window x:Class="ImperialShield.Views.WhitelistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🛡️ Lista Blanca - Imperial Shield" 
        Height="650" Width="850"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource ModernWindow}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Padding="25" Background="{StaticResource EpicHeaderGradient}" BorderBrush="#4DA8DA" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🛡️" FontSize="36" Margin="0,0,20,0" VerticalAlignment="Center"/>
                <StackPanel>
                    <TextBlock Text="LISTA BLANCA DE APLICACIONES" FontSize="22" FontWeight="Bold" Foreground="#4DA8DA"/>
                    <TextBlock Text="Excluye aplicaciones confiables del monitoreo de red anómala (DDoS)" Foreground="#94A3B8"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                
                <!-- ═══════════════ WHITELIST MANAGEMENT SECTION ═══════════════ -->
                <Border Style="{StaticResource RPGCard}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="📝" FontSize="28" Margin="0,0,15,0" VerticalAlignment="Center"/>
                            <StackPanel>
                                <TextBlock Text="GESTIÓN DE APLICACIONES PERMITIDAS" FontSize="18" FontWeight="Bold" Foreground="#4DA8DA"/>
                                <TextBlock Text="Añade programas para evitar que activen la alerta de actividad de red anómala" Foreground="#94A3B8" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Add New -->
                        <Border Grid.Row="1" Background="#0D1B2A" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBox x:Name="NewExeTextBox" 
                                         Background="#1B2838" Foreground="White" 
                                         BorderBrush="#3A5F82" BorderThickness="1"
                                         Padding="12,10" FontSize="14" VerticalContentAlignment="Center"
                                         Text="" ToolTip="Nombre del ejecutable (ej: chrome.exe)">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox">
                                            <Style.Triggers>
                                                <Trigger Property="Text" Value="">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                                 <VisualBrush.Visual>
                                                                     <TextBlock Text="  Nombre del ejecutable (ej: app.exe)" 
                                                                                Foreground="#555" FontStyle="Italic" Padding="10,0"/>
                                                                 </VisualBrush.Visual>
                                                            </VisualBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                                
                                <Button Grid.Column="1" Content="📂" Click="BrowseExe_Click" 
                                        Width="50" Height="42" Margin="10,0,0,0"
                                        Style="{StaticResource ModernButton}" Background="#3A5F82"
                                        ToolTip="Buscar archivo ejecutable"/>
                                
                                <Button Grid.Column="2" Content="➕ AÑADIR" Click="AddWhitelist_Click" 
                                        Width="140" Height="42" Margin="10,0,0,0"
                                        Style="{StaticResource RPGSuccessButton}" FontSize="13"/>
                            </Grid>
                        </Border>

                        <TextBlock Grid.Row="2" Text="Aplicaciones Excluidas:" 
                                   Foreground="#E8F4FC" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>

                        <!-- Whitelisted List -->
                        <DataGrid x:Name="WhitelistGrid" Grid.Row="3" 
                                  AutoGenerateColumns="False" IsReadOnly="True"
                                  Background="Transparent" RowBackground="#0D1B2A" AlternatingRowBackground="#162232"
                                  HeadersVisibility="Column" GridLinesVisibility="None" BorderThickness="0"
                                  MinHeight="200" MaxHeight="300">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="Nombre del Proceso / Ejecutable" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <TextBlock Text="🛡️" FontSize="16" Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                
                                <DataGridTemplateColumn Header="Acciones" Width="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="🗑️ REMOVER" Click="RemoveWhitelist_Click"
                                                    Style="{StaticResource RPGDangerButton}" 
                                                    FontSize="11" Padding="12,6" Tag="{Binding Name}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <TextBlock x:Name="EmptyListText" Grid.Row="3" 
                                   Text="No hay aplicaciones en la lista blanca" 
                                   Foreground="#555" FontStyle="Italic" FontSize="14"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Visibility="Collapsed"/>
                    </Grid>
                </Border>

                <!-- Info Panel -->
                <Border Background="#16202C" CornerRadius="8" Padding="20" Margin="0,20,0,0" BorderBrush="#3A5F82" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="ℹ️ ¿Qué es la Lista Blanca de Red?" Foreground="#4DA8DA" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                        <TextBlock TextWrapping="Wrap" Foreground="#94A3B8" FontSize="12" LineHeight="20">
                            <Run>Al añadir una aplicación a la lista blanca, Imperial Shield omitirá la verificación de conexiones simultáneas y volumen para ese proceso.</Run>
                            <LineBreak/><LineBreak/>
                            <Run>Esto es útil para:</Run>
                            <LineBreak/>
                            <Run>• Navegadores web que uses frecuentemente (como Arc, Chrome, Firefox).</Run>
                            <LineBreak/>
                            <Run>• Clientes de juegos, launchers (Steam, Epic Games) o gestores de descarga legítimos.</Run>
                            <LineBreak/>
                            <Run>• Herramientas de desarrollo que abran múltiples sockets locales o remotos.</Run>
                            <LineBreak/><LineBreak/>
                            <Run FontStyle="Italic" Foreground="#F39C12">⚠️ Nota: Evita añadir ejecutables desconocidos a esta lista, ya que podrían encubrir ataques salientes.</Run>
                        </TextBlock>
                    </StackPanel>
                </Border>
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
