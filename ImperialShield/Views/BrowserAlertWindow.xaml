<Window x:Class="ImperialShield.Views.BrowserAlertWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ALERTA DE SEGURIDAD - Imperial Shield" 
        Width="600" Height="580"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Storyboard x:Key="PulseIcon">
            <DoubleAnimation Storyboard.TargetName="AlertIcon" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="1.0" To="1.1" Duration="0:0:0.8" AutoReverse="True" RepeatBehavior="Forever"/>
            <DoubleAnimation Storyboard.TargetName="AlertIcon" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="1.0" To="1.1" Duration="0:0:0.8" AutoReverse="True" RepeatBehavior="Forever"/>
        </Storyboard>
        
        <Storyboard x:Key="WindowEntrance">
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="0.8" To="1.0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <BackEase Amplitude="1" EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="0.8" To="1.0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <BackEase Amplitude="1" EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>
    
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource WindowEntrance}"/>
            <BeginStoryboard Storyboard="{StaticResource PulseIcon}"/>
        </EventTrigger>
    </Window.Triggers>
 
    <Border x:Name="MainBorder" CornerRadius="30" Background="#0A0F1E" BorderBrush="#F39C12" BorderThickness="4" Opacity="0" UseLayoutRounding="True">
        <Border.RenderTransform>
            <ScaleTransform CenterX="300" CenterY="290"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="60" ShadowDepth="0" Opacity="0.7" Color="#F39C12"/>
        </Border.Effect>
        
        <Grid Margin="35">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Alert Icon -->
            <Image x:Name="AlertIcon" Source="/Resources/shield_alert_icon.png" 
                   Width="100" Height="100" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,15">
                <Image.RenderTransform>
                    <ScaleTransform CenterX="50" CenterY="50"/>
                </Image.RenderTransform>
                <Image.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Color="#F43F5E" Opacity="0.9"/>
                </Image.Effect>
            </Image>
            
            <TextBlock Grid.Row="1" Text="¡CAMBIO DE NAVEGADOR DETECTADO!" FontSize="24" FontWeight="ExtraBold" Foreground="#F39C12" 
                       HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,5">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="2" Color="Black" Opacity="0.5"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Grid.Row="1" Text="Se ha detectado un cambio en el navegador predeterminado del sistema." FontSize="14" Foreground="#94A3B8" 
                       HorizontalAlignment="Center" TextAlignment="Center" Margin="0,35,0,0"/>

            <Border Grid.Row="2" Background="#111827" CornerRadius="12" Margin="0,20" Padding="20" BorderBrush="#3A5F82" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="NAVEGADOR ANTERIOR:" FontSize="10" Foreground="#64748B" FontWeight="Bold"/>
                    <TextBlock x:Name="OldBrowserText" Text="Desconocido" FontSize="16" Foreground="#94A3B8" TextWrapping="Wrap" Margin="0,0,0,15"/>
                    
                    <TextBlock Text="NUEVO NAVEGADOR:" FontSize="10" Foreground="#F59E0B" FontWeight="Bold"/>
                    <TextBlock x:Name="NewBrowserText" Text="Nuevo Navegador" FontSize="20" Foreground="White" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,5"/>
                    <TextBlock x:Name="NewBrowserIdText" Text="ProgId" FontSize="11" Foreground="#4DA8DA" FontStyle="Italic"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="3" Orientation="Vertical" HorizontalAlignment="Center">
                <Button x:Name="RestoreBtn" Content="REVERTIR CAMBIO" Width="420" Height="50" 
                        Background="#E74C3C" Foreground="White"
                        BorderThickness="0" 
                        FontSize="16" FontWeight="Bold" Cursor="Hand"
                        Margin="0,0,0,10" Click="Restore_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#C0392B"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="15" ShadowDepth="4" Color="Black" Opacity="0.4"/>
                    </Button.Effect>
                </Button>

                <Button x:Name="IgnoreBtn" Content="IGNORAR CAMBIO" Width="420" Height="45" 
                        Background="Transparent" Foreground="#94A3B8"
                        BorderThickness="1" BorderBrush="#3A5F82" 
                        FontSize="14" FontWeight="Bold" Cursor="Hand"
                        Click="Ignore_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderBrush" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
